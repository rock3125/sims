#pragma once

#include <entt/entt.hpp>

namespace sims {

// Type alias so we can swap the registry type in one place later.
using Registry = entt::registry;
using Entity = entt::entity;

} // namespace sims
